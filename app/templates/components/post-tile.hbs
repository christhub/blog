<!-- {{#if hovering}}
  <p>hovered!</p>
{{else}}
  <p {{action "something" on="mouseEnter"}}>testing something</p>
{{/if}}
 -->

<div class="col-xs-4">
    <div class="post">
        <img class="post-image" src='{{post.photo}}' {{action 'openModal' post}}>
      <div>
        <h2 id="post">{{post.headline}}</h2>
      </div>

    </div>
</div>

{{post-modal post=post}}

{{yield}}
